[package]
name = "webrtc-apm-java"
version = "0.1.0"
edition = "2021"

[lib]
name = "apm"
crate-type = ["cdylib"]

[dependencies]
webrtc-audio-processing = { version = "=2.0.3", features = ["bundled"] }
webrtc-audio-processing-config = "=2.0.3"
